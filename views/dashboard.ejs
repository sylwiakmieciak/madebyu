<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - MadeByU</title>
    <link rel="stylesheet" href="/css/variables.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <main>
        <section style="padding: 3rem 0; margin-top: 80px;">
            <div class="container">
                <% if (typeof welcome !== 'undefined' && welcome) { %>
                    <div class="alert alert-success" style="margin-bottom: 2rem;">
                        Witaj, <%= user.username %>! Twoje konto zostalo utworzone.
                    </div>
                <% } %>

                <h1 style="margin-bottom: 2rem;">Dashboard - <%= user.username %></h1>
                
                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 2rem; margin-top: 2rem;">
                    <div style="background: white; padding: 2rem; border-radius: 12px; box-shadow: 0 4px 20px rgba(93, 78, 55, 0.08);">
                        <h3>Email</h3>
                        <p><%= user.email %></p>
                    </div>
                    
                    <div style="background: white; padding: 2rem; border-radius: 12px; box-shadow: 0 4px 20px rgba(93, 78, 55, 0.08);">
                        <h3>Rola</h3>
                        <p><%= user.role %></p>
                    </div>
                    
                    <div style="background: white; padding: 2rem; border-radius: 12px; box-shadow: 0 4px 20px rgba(93, 78, 55, 0.08);">
                        <h3>Metoda logowania</h3>
                        <p><%= user.oauth_provider %></p>
                    </div>
                    
                    <div style="background: white; padding: 2rem; border-radius: 12px; box-shadow: 0 4px 20px rgba(93, 78, 55, 0.08);">
                        <h3>Data rejestracji</h3>
                        <p><%= new Date(user.created_at).toLocaleDateString('pl-PL') %></p>
                    </div>
                </div>

                <div style="margin-top: 3rem;">
                    <h2 style="margin-bottom: 1.5rem;">Szybkie akcje</h2>
                    <div style="display: flex; gap: 1rem; flex-wrap: wrap;">
                        <a href="/products/new" class="btn btn-primary">Dodaj produkt</a>
                        <a href="/my-products" class="btn btn-secondary">Moje produkty</a>
                        <a href="/orders" class="btn btn-secondary">Zamowienia</a>
                        <a href="/profile/edit" class="btn btn-secondary">Edytuj profil</a>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 MadeByU. Wszystkie prawa zastrzezone.</p>
            <div class="footer-links">
                <a href="/about">O nas</a>
                <a href="/contact">Kontakt</a>
                <a href="/terms">Regulamin</a>
            </div>
        </div>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>
